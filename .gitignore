# Personal deployment values (Apps Script URL + sync token). Never commit.
MY-SETUP.local.md
js/config.local.js

# multi-llm working notes -- development process, not part of the project
multi-llm-state.md

# Fetched lesson material. Data, not code: regenerate with
# `node tools/fetch-materials.js`, and it lives in the sheet once uploaded.
materials*.json

# OS / editor noise
Thumbs.db
.DS_Store
.vscode/
node_modules/
